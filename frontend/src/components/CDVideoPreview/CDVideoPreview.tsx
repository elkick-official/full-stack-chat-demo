import React, { FunctionComponent, useEffect, useRef } from "react";

interface ICDVideoPreviewProps {
  videoUrl: string;
}
const CDVideoPreview: FunctionComponent<ICDVideoPreviewProps> = ({
  videoUrl,
}) => {
  return <div></div>;
};

export default CDVideoPreview;
